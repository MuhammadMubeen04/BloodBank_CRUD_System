# 🩸 Blood Bank Management System (C++ & MySQL)

## 📖 Overview
The **Blood Bank Management System** is a database-driven C++ project designed to manage donor and recipient information efficiently.  
It connects to a MySQL database and supports full **CRUD operations** (Create, Read, Update, Delete).  
Developed as part of the **Database Lab Project**, it demonstrates how database connectivity works using C++ and MySQL.

---

## 🎯 Features
- ➕ Add new donor and recipient records  
- 🔍 View and search existing data  
- ✏️ Update donor or recipient details  
- ❌ Delete records securely  
- 🗄️ MySQL backend for persistent data storage  
- 💻 Simple command-line interface for easy use  

---

## 🛠️ Tools & Technologies
- **Language:** C++  
- **Database:** MySQL  
- **Library:** `libmysql.dll` (MySQL Connector)  
- **IDE:** Dev-C++ / Code::Blocks (Windows)  

---

## ⚙️ Setup Instructions
1. Install **MySQL Server** and create a database named `bloodbank_db`.  
2. Import the provided SQL file (if available) or create tables manually.  
3. Open the project in your preferred IDE (Dev-C++ or Code::Blocks).  
4. Link the **MySQL library (`libmysql.dll`)** for database connection.  
5. Compile and run `main.cpp` to start the application.  

---

## 🧩 File Structure
